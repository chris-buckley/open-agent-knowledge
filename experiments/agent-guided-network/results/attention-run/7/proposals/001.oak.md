<instructions>
AS binds one constant or state value to one schema placeholder; the value must satisfy that placeholder at resolution and before each state write commits.
Constants hold values that do not change while the knowledge runs.
Each schema is one information shape: a template with <PLACEHOLDER> slots and WHERE lines that constrain each slot.
</instructions>

<constants>
baseline AS schema.proposal.BASELINE: "9aba2b10d4fd04cbd519c6d5d35b14a50b707506da00532a1d658171245ddc39"

observation AS schema.proposal.OBSERVATION: "217e585887a0e5883250ad5d98d87d9d073f0513e3d091a01efed0a06ddea4a3"

owner AS schema.proposal.OWNER: "attention-network"

method AS schema.proposal.METHOD: "cool-output"

rationale AS schema.proposal.RATIONALE: "Training accuracy is 100% with loss 0.00184, but development accuracy is 91.80% with loss 0.41366. Retrieval is imperfect, especially at the second block. First halve only the second output projection to reduce overconfident class logits without changing attention or argmax decisions. This tests calibration separately from retrieval and consumes no gradient steps."

actor AS schema.proposal.ACTOR: "assistant"
</constants>

<schemas>
<schema id="proposal">
baseline: <BASELINE>
observation: <OBSERVATION>
owner: <OWNER>
method: <METHOD>
rationale: <RATIONALE>
actor: <ACTOR>

WHERE:
- <BASELINE> is string; is non-empty.
- <OBSERVATION> is string; is non-empty.
- <OWNER> is string; is non-empty.
- <METHOD> is string; is non-empty.
- <RATIONALE> is string; is non-empty.
- <ACTOR> is string; is non-empty.
</schema>
</schemas>