<instructions>
AS binds one constant or state value to one schema placeholder; the value must satisfy that placeholder at resolution and before each state write commits.
Constants hold values that do not change while the knowledge runs.
Each schema is one information shape: a template with <PLACEHOLDER> slots and WHERE lines that constrain each slot.
</instructions>

<constants>
baseline AS schema.proposal.BASELINE: "1cff7e2806e8f8083e6fb6b61abffa460017cc2008e5ca20fab9f8b2dbbb22eb"

observation AS schema.proposal.OBSERVATION: "876007929f56eac6b2322f92981bbc7ddc1c986d7356727eb24c6d1524dd8bcb"

owner AS schema.proposal.OWNER: "attention-network"

method AS schema.proposal.METHOD: "first"

rationale AS schema.proposal.RATIONALE: "Refitting only the second block improved development accuracy slightly but doubled cross-entropy, so it was rejected. Correct first-key top1 does not guarantee a useful bridge vector because value/output projections can distort it. For the final proposal, freeze the second block and fit only the first block through the end-to-end label loss for 150 steps. This tests improving the representation presented to the existing readout rather than sharpening weights or increasing readout confidence."

actor AS schema.proposal.ACTOR: "assistant"
</constants>

<schemas>
<schema id="proposal">
baseline: <BASELINE>
observation: <OBSERVATION>
owner: <OWNER>
method: <METHOD>
rationale: <RATIONALE>
actor: <ACTOR>

WHERE:
- <BASELINE> is string; is non-empty.
- <OBSERVATION> is string; is non-empty.
- <OWNER> is string; is non-empty.
- <METHOD> is string; is non-empty.
- <RATIONALE> is string; is non-empty.
- <ACTOR> is string; is non-empty.
</schema>
</schemas>