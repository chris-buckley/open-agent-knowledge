# Format assets

This skill does not require a reusable APS output-format contract. The scripts emit documented text or JSON directly, and their output records are described in the references.

Do not duplicate diagnostic rules here. Add a versioned APS format asset only when an agent process needs a new stable human-readable rendering contract.
