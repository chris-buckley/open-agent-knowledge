# Constants assets

This skill does not require a reusable APS constants block. The directory remains present because the governing skill template reserves `assets/constants/` for future structured constants.

Do not duplicate JSON Schema keyword rules here. Add an asset only when a process needs a stable machine-readable constant set that is not already represented by a schema or source manifest.
