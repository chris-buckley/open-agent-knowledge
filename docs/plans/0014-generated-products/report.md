# Generated product consolidation report

Status: Complete. Delivered in PR #22; implementation CI passed. Not merged.
Baseline: `main` at `2a3ba6c27b6110de4ace334e51d2f195a7ded522`.
Plan: [Consolidate generated OAK products](plan.md).
Branch: `docs/plan-generated-products`.
Authorisation: The user approved implementation and a pull request on 2026-09-06 at 22:09:47+10:00. No merge is authorised.

## Outcome

The generated grammar, construct definitions, standalone authoring agent, and modular bundle now share one generated-only product root:

```text
generated/
├── oak.ebnf
├── definitions/
│   └── <construct>.oak.md
├── oak-authoring.oak.md
└── oak-authoring.skill/
    ├── SKILL.md
    ├── references/
    ├── guides/
    ├── assets/
    ├── scripts/validate.py
    └── _template/
```

`oak-authoring.skill` is a directory, not an archive format. Its metadata remains `name: oak-authoring`. Install its contents in a directory named `oak-authoring` when the host requires the Agent Skills directory/name match. This follows the [Agent Skills naming requirement](https://agentskills.io/specification#name-field); it does not claim that a dot-suffixed directory is a standard installed skill name.

## Source and destination map

| Former location | Current location | Responsibility |
| --- | --- | --- |
| `outputs/oak.ebnf` | `generated/oak.ebnf` | Generated grammar; unchanged bytes |
| `outputs/docs/*.md` | `generated/definitions/*.oak.md` | OAK definitions of individual language constructs |
| `build/docs.py` | `build/definitions.py` | Source-owned definition generator |
| `outputs/oak-authoring.oak.md` | `generated/oak-authoring.oak.md` | Assembled authoring agent; unchanged bytes |
| `skills/oak-authoring/` | `generated/oak-authoring.skill/` | Modular bundle; existing delivered bytes unchanged |
| `skills/oak-authoring/scripts/validate.py` | `build/authoring_validator.py` | Sole maintained helper and identity source |
| Same maintained helper | `generated/oak-authoring.skill/scripts/validate.py` | Generated byte-identical standalone helper |
| `outputs/AGENTS.md`, `skills/AGENTS.md` | `build/AGENTS.md` | Consolidated source-owned product and capability guidance |

The complete per-file map, original byte sizes, and hashes are recorded in [baseline-products.json](evidence/baseline-products.json). No root `outputs/` or `skills/` remains. `.agents/skills/`, repository planning documents, and example sources/siblings retain their existing roles.

## Required comparisons

| Comparison | Observation | Evidence |
| --- | --- | --- |
| E01: Product layout | Exactly four top-level product entries and 69 generated files; no source guidance in the tree | [Generated manifest](evidence/generated-manifest.json), [audit result](evidence/audit.json) |
| E02: Source ownership | Source helper is outside generated; all helper bytes and immutable identity values are preserved; guidance is consolidated under build | Source diff, scoped AGENTS checks, [audit.py](evidence/audit.py) |
| E03: Construct definitions | All 38 pages have `.oak.md` names; each page changes only its `syntax-reference` from `outputs/oak.ebnf` to `generated/oak.ebnf` | Frozen baseline hash audit; `build/definitions.py` |
| E04: Regeneration | Fresh interpreter processes rebuild every product without the generated or old delivery trees, repeat exactly, repair missing/edited copies, and reject escaping links without changing source inputs | `build/checks/outputs.py`, [verification](evidence/verification.json) |
| E05: Portable behaviour | Detached installed-name skill and standalone agent resolve without sibling definitions or repository build imports; parity, fusion refusals, fingerprints, no-install outcomes, and grammar equality checks pass | Full verification entry points, detached checks, optional-validator checks |

The standalone agent is 63,995 bytes against the unchanged 64,000-byte limit. The skill entry is 8,107 bytes against the unchanged 10,000-byte limit. The grammar, all existing modular skill contents, teaching payloads, template, and helper are byte-identical to baseline. No package, dependency, schema, syntax, execution, or language-version change is required.

## Verification

The repository was exported by GitHub Actions at `2c9276dd4a650d7017f82d1a80da1d9b6ae26516`. Reconstructing the exported tracked tree locally produced exactly Git tree `54cfe6a921e7d162fcb87d8ee5b8335874a644d2`. The baseline full check passed before edits. The local Git snapshot commit is verification scaffolding only and is not part of repository history.

Observed local commands:

| Command | Result |
| --- | --- |
| `python -m compileall -q oak build examples generated/oak-authoring.skill/scripts` | Exit 0 |
| `python -m examples.catalog` | Exit 0 |
| `python -m build.ebnf` | Exit 0 |
| `python -m build.definitions` | Exit 0 |
| `python -m build.authoring` | Exit 0 |
| `python -m build.examples` | Exit 0 |
| `python build/examples.py` | Exit 0 |
| Repeat all generators and compare complete product hashes | Identical 69-file manifest |
| `python docs/plans/0014-generated-products/evidence/audit.py` | Passed |

[verification.json](evidence/verification.json) records actual commands, exit codes, output, and environment. The audit imports no generator or runtime package and compares output against frozen pre-migration hashes, normalising only the one explicitly permitted definition-path literal. It is an independent expectation check, not a claim of review by another person or model.

New checks cover missing and edited generated helpers, missing grammar, stale definition files with either suffix, unowned directories, symbolic-link files and directories, preservation of maintained inputs, adjacent validator discovery at the new delivery location, detached installed-name validation, and absence of old roots. Existing language, shape, authoring parity, consent, identity, and scope checks were retained.

## Review and scope

The [path audit](evidence/path-audit.json) distinguishes process-model `outputs` field names from paths. The only remaining old-root path mentions in active code are intentional absence assertions. The OAK runtime, `.agents` knowledge, dependency declaration, and legacy APS tree are unchanged. Root routing, build guidance, generation/check code, verification workflow, the example guidance pointer, and this active plan/evidence are updated together.

One historical navigational link in plan 0013 now points to its original revision of `outputs/AGENTS.md`, because that source document is removed. Historical before-state text and earlier completion claims are not rewritten.

The shared generated-file writer preflights destinations and the existing generated tree before mutation, rejects symlinks and special files, and prunes only the owning generated subtree. Runtime bytecode caches remain outside product manifests and are preserved. The optional validator itself is not rewritten or weakened.

## Limitations and delivery gate

Direct GitHub DNS resolution is unavailable in this container. Repository transport uses the GitHub connector and Actions; remote history must retain the original planning and export commits. The temporary transport workflow is removed from the delivered tree.

Local verification uses an external Python interpreter with installed declared dependencies and no editable repository installation. The existing explicitly approved download/bootstrap integration remains in GitHub CI; no local network-bootstrap success is claimed here.

Local verdict: Approved for remote candidate verification. This is not a merge approval. Remote commit, pull request, and CI observations are recorded below only after they exist.

## Corrected remote finding

The first remote candidate run, [34034104544](https://github.com/chris-buckley/open-agent-knowledge/actions/runs/34034104544), reproduced the expected Git tree but failed before publication. Its fresh Python 3.13.15 interpreter imported SSL after the test had replaced `socket.socket` with a function; SSL subclasses that class and raised `TypeError: function() argument 'code' must be code, not str`.

Both isolated test programs now block socket audit events without replacing the socket class. Each imports SSL after installing the guard and asserts that socket construction is rejected. This is a no-network regression guard for trusted test code, not a security sandbox. The focused product checks and both complete local entry points passed again after the correction, with unchanged generated hashes. The OAK runtime and optional validator were not changed by this fix.

## Remote delivery

Implementation commit: `46d141ed5d3052cee77c2b119d70d8b7b4113fb8`.
Verified implementation tree: `8759292b5c0e52fbe094c6c94a0366bd7e9a4f92`.
Pull request: [#22 into main](https://github.com/chris-buckley/open-agent-knowledge/pull/22).

The [corrected candidate run 34034557254](https://github.com/chris-buckley/open-agent-knowledge/actions/runs/34034557254) passed both complete verification entry points, the frozen-baseline audit, repeated generation, and exact Git-tree checks. Its final push was rejected because the Actions token could not update `verify.yml`. The repository connector then fast-forwarded the work branch to the same verified commit, preserving its identity and every original ancestor. No verification failure is concealed by that transport recovery.

The normal [Verify OAK run 34034731684](https://github.com/chris-buckley/open-agent-knowledge/actions/runs/34034731684) passed for the implementation in PR #22. This includes compilation, both complete entry points, fresh generation without a diff, and the approved detached validator bootstrap: refusal without consent, pinned download and isolated installation with consent, and retained cache reuse without the permission flag.

All 24 plan tasks are checked, and E01 through E05 are supported by the recorded evidence. The closeout changes only the plan and this report; product and implementation bytes remain those of the verified tree. Temporary transport files and workflows are absent from the delivered product change. Final verification verdict: Approved. This verdict does not authorise merging; that remains a separate repository-owner decision.
