# Structured-first scoped OAK AGENTS migration plan

```yaml
status: implementation
repository: chris-buckley/open-agent-knowledge
branch: docs/scoped-oak-agents
baseline_branch: main
baseline_commit: 9791ba43f881957325f18cd1685b2f485e4c9867
change_type: architecture-authority-dogfooding
```

Replace parallel manuals with scoped canonical OAK `AGENTS.md` documents. Model knowledge through structured parts first. Add authored instructions only as a justified last resort. Keep every agent file at or below 500 lines.

## Scoped graph

```text
AGENTS.md
oak/AGENTS.md
oak/node/AGENTS.md
oak/resolve/AGENTS.md
oak/execute/AGENTS.md
build/AGENTS.md
examples/AGENTS.md
outputs/AGENTS.md
docs/AGENTS.md
```

## Meaning

- [x] Make root `AGENTS.md` the global owner and scoped router.
- [x] Give each nested `AGENTS.md` one directory concern.
- [x] Treat hierarchy application as host scoping, not OAK imports.
- [x] Keep exact fields, syntax, codes, and runtime behavior in the package.
- [x] Keep generated outputs as reference and completed plans as history.
- [x] Remove parallel current architecture and authoring-guide ownership.

## Structured-first OAK

- [x] Make every repository-owned `AGENTS.md` one canonical complete OAK document.
- [x] Store ownership, maps, limits, policies, and invariants as constants.
- [x] Define root task and result contracts as schemas.
- [x] Route repository requests and merge events through triggers.
- [x] Encode repository work as processes.
- [x] Define root task input and result output as interfaces.
- [x] Omit state because no persistent AGENTS runtime value is justified.
- [x] Put `instructions` last in the root part-authoring priority.
- [x] Use zero authored instructions in the current graph.
- [x] Permit future authored instructions only with one explicit justification constant.
- [x] Keep generated interpretation instructions automatic and outside authored-content checks.
- [x] Keep every `AGENTS.md` at or below 500 lines.

## Scoped ownership

- [x] Put package and representation knowledge in `oak/AGENTS.md`.
- [x] Put node, part, lifetime, schema, and local-validation knowledge in `oak/node/AGENTS.md`.
- [x] Put target and graph knowledge in `oak/resolve/AGENTS.md`.
- [x] Put runtime and transaction knowledge in `oak/execute/AGENTS.md`.
- [x] Put generation, checks, freshness, and prompt knowledge in `build/AGENTS.md`.
- [x] Put practical authoring and example conventions in `examples/AGENTS.md`.
- [x] Put generated-only rules in `outputs/AGENTS.md`.
- [x] Put plan and report rules in `docs/AGENTS.md`.
- [x] Delete `docs/architecture` and `docs/guides`.
- [x] Keep older completed plans unchanged as historical evidence.

## No duplication

- [x] Give each architecture fact one physical owner.
- [x] Keep the root router as data instead of child summaries.
- [x] Reject duplicate normalized authored claims across the graph.
- [x] Reject child claims repeated from an ancestor.
- [x] Inspect the graph for paraphrased competing ownership.

## Verification

- [x] Replace the manual-documentation check with a scoped structured-AGENTS check.
- [x] Discover the exact current path set.
- [x] Check canonical parse and render equality.
- [x] Check the 500-line maximum against the root constant.
- [x] Check root router coverage and order.
- [x] Check structured content exists before authored instructions.
- [x] Check root schemas, triggers, processes, and interfaces.
- [x] Reject obsolete PRD, manuals, guides, README indexes, and checker paths.
- [x] Keep `outputs/AGENTS.md` outside the generated artifact set.
- [x] Run source compilation and every generator locally.
- [x] Run `python -m build.examples` locally.
- [x] Run `python build/examples.py` locally.
- [x] Confirm repeated generation leaves no output diff locally.
- [x] Run `git diff --check` locally.
- [ ] Pass independent GitHub-hosted verification on the structured implementation.
- [ ] Remove temporary validation support.
- [ ] Complete `docs/plans/0003-report.md` with final evidence and verdict.
- [ ] Mark every completion item done only after remote verification passes.

## Completion standard

- [ ] Nine scoped `AGENTS.md` files form one structured canonical OAK knowledge graph.
- [ ] No file exceeds 500 lines.
- [ ] No current parallel architecture manual or authoring guide remains.
- [ ] No authored instruction is present without explicit justification.
- [ ] No authored claim is duplicated across the graph.
- [ ] Root routing and ownership are exact.
- [ ] Full verification passes with no stale output.
- [ ] Final report verdict is `Approved`.
