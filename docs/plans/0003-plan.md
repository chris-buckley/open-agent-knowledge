# Scoped OAK AGENTS migration plan

```yaml
status: implementation
repository: chris-buckley/open-agent-knowledge
branch: docs/scoped-oak-agents
baseline_branch: main
baseline_commit: 9791ba43f881957325f18cd1685b2f485e4c9867
change_type: architecture-authority-dogfooding
```

Replace parallel architecture manuals with one scoped graph of canonical OAK `AGENTS.md` files. Give every current fact one owner and keep every agent file at or below 500 lines.

## Accepted graph

```text
open-agent-knowledge/
├── AGENTS.md
├── oak/
│   ├── AGENTS.md
│   ├── node/AGENTS.md
│   ├── resolve/AGENTS.md
│   └── execute/AGENTS.md
├── build/AGENTS.md
├── examples/AGENTS.md
├── outputs/AGENTS.md
└── docs/
    ├── AGENTS.md
    └── plans/
```

## Authority

- [ ] Make root `AGENTS.md` the sole global rule owner and scoped router.
- [ ] Make each nested `AGENTS.md` the sole owner of one directory concern.
- [ ] Treat hierarchy application as host scoping, not OAK import semantics.
- [ ] Keep exact fields, syntax, codes, and runtime behaviour in the package.
- [ ] Keep outputs generated and plans historical.
- [ ] Remove parallel current architecture and guide ownership.

## Canonical OAK

- [ ] Render every repository-owned `AGENTS.md` as canonical XML-grouped OAK.
- [ ] Use the instructions part only.
- [ ] Start every file with one sole-ownership instruction.
- [ ] Keep every file at or below 500 lines.
- [ ] Keep the root router as visible tree entries.
- [ ] Remove Markdown wrappers, headings, tables, and concepts CSV from current AGENTS files.
- [ ] Preserve no decorative or empty OAK part.

## Scoped content

- [ ] Move package and representation architecture to `oak/AGENTS.md`.
- [ ] Move node, part, value-lifetime, schema, and local-validation architecture to `oak/node/AGENTS.md`.
- [ ] Move target-path and graph architecture to `oak/resolve/AGENTS.md`.
- [ ] Move execution and transaction architecture to `oak/execute/AGENTS.md`.
- [ ] Move generator, check, freshness, and authoring-prompt architecture to `build/AGENTS.md`.
- [ ] Move practical authoring and executable-example rules to `examples/AGENTS.md`.
- [ ] Move generated-only rules to `outputs/AGENTS.md`.
- [ ] Move plan and report rules to `docs/AGENTS.md`.
- [ ] Delete `docs/architecture` and `docs/guides`.
- [ ] Retain completed plans without rewriting their historical references.

## No duplication

- [ ] Give each architecture fact one physical owner.
- [ ] Keep parents as routes rather than child summaries.
- [ ] Remove exact duplicate authored instructions across the graph.
- [ ] Reject an instruction repeated from an ancestor.
- [ ] Keep generated interpretation text outside authored duplication checks.
- [ ] Inspect the complete graph for paraphrased duplicate ownership.

## Verification

- [ ] Replace the manual-documentation freshness check with a scoped AGENTS graph check.
- [ ] Discover the exact current AGENTS path set.
- [ ] Check canonical parse and render equality.
- [ ] Check the 500-line limit.
- [ ] Check root router coverage and sole ownership.
- [ ] Check duplicate normalized instructions.
- [ ] Reject current PRD, architecture manuals, guides, README indexes, and obsolete check paths.
- [ ] Keep generated output path checks compatible with `outputs/AGENTS.md`.
- [ ] Run `python -m compileall -q oak build examples`.
- [ ] Run `python -m build.examples`.
- [ ] Run `python build/examples.py`.
- [ ] Confirm repeated generation changes no output.
- [ ] Run `git diff --check`.
- [ ] Inspect the final diff and obsolete-path search.
- [ ] Remove temporary workflow support.
- [ ] Write `docs/plans/0003-report.md` with final evidence and verdict.
- [ ] Mark this plan complete only after every applicable check passes.

## Completion standard

- [ ] Nine repository-owned AGENTS files form one scoped canonical OAK graph.
- [ ] No AGENTS file exceeds 500 lines.
- [ ] No current architecture manual or authoring guide remains under `docs`.
- [ ] No authored instruction is duplicated across the graph.
- [ ] Root routing and nested ownership are exact.
- [ ] Full repository verification passes with no stale output.
- [ ] Final report verdict is `Approved`.
