# Agent-guided network design delivery

Date: 2026-09-05.
Scope: Experiment design and repository structure only.
Plan: [SMEAC execution record](plan.md).
Specification: [EXPERIMENT.md](../../../experiments/agent-guided-network/EXPERIMENT.md).
Baseline: `cd1f8aed74b24f8515a3e176972e9f2cbcb53e5a`.
Branch: `experiment/agent-guided-network`.

## Outcome

The design preserves the user's intent: agents help revise matrices owned by OAK nodes during learning, then are removed while the learned numerical network runs alone. The specification covers numerical-first execution, immutable revisions, meaningful modules, behavioural proposals and numerical fitting, credit assignment, coordinated acceptance, independent evaluation, export closure, research precedents, and risks.

The package separates node contracts, training, numerical export, benchmark design, primary sources, and evidence status. The plan keeps future implementation tasks open and explicitly gated. No runtime, training integration, benchmark result, trained weights, or export artifact is represented as implemented.

## Changed paths

New experiment material is under `experiments/`, with this plan and report under `docs/plans/0008-agent-guided-network/`. Root routing and the existing AGENTS discovery inventory require the matching experiment-owner entry. No OAK grammar or execution semantics are changed.

## Verification evidence

The root and applicable scoped owners were read before repository changes. The branch was created through the connected GitHub tool from the baseline above. Research titles, primary-source abstracts, and official export documentation were checked on 5 September 2026.

Local repository cloning was attempted and failed with `Could not resolve host: github.com`. Local draft checks can verify the authored documents but are not execution of the repository. Repository-wide checks must be reported only if actually observed on another execution surface.

Final document checks, commit identity, and any repository CI result are recorded after publication. At this drafting stage they are not claimed complete.

## Experimental evidence

None. [Evidence status](../../../experiments/agent-guided-network/results/STATUS.md) explicitly records that training, benchmarking, and export have not been run. Repository validation, if successful, is not a scientific result.

## Verdict

Design package prepared for verification and publication. Implementation and experimental verdicts remain open; main is not a delivery target for this request.
