# D03 artifact milestone review

Status: D01 through D03 implemented and checked. This is checkpoint 02, not full-plan completion or a PR receipt.
Base: `88340b4bde9d58295e881884a80a0a99c247e13a`.
Governing revision: `9956e6998869fcfbd84067eec0d6303273a54174`.
Recovery task: `82d0f13f-f86a-46bb-9f24-74f5d94bd5dd`.

## Recovered authority and source

The controller-supplied outer package, nested original inputs, local Git bundle and checkpoint 01 were checked against their declared byte counts and SHA-256 identities. A fresh local clone supplied the exact base and complete Git history without a network fetch. The original source manifest covered 598 files; applying checkpoint 01 reproduced all 604 checkpoint hashes. Four of the checkpoint's five worker/scenario files remain byte-identical in this milestone. The explorer Python source has one flat-construction correction described below; its canonical OAK and all other worker/scenario bytes remain unchanged. The supplied controller receipt records the earlier synchronized parallel check against that exact checkpoint, not this later source tree.

Later individual files were mixed recovery candidates. Their declared hashes were checked; recorded subsequent edits supplied missing artifact-test and catalogue changes. The final implementation is regenerated and retested as one tree. The initial reconciliation note and intermediate failed checks remain in the checkpoint evidence so that a superseded observation cannot be mistaken for current verification. No earlier successful lifecycle checkpoint is invented and root state declarations are not rewritten.

D01.01 is evidenced by the existing plan-amendment commit at the base. It was not repeated. The authority is the artifact-only amendment, not the historical MCP/runtime research or preflight blocker.

## Source and delivered meaning

The native delivery is exactly five text/TOML files under `generated/oak.agents`: the portable coordinator, full explorer, sample knowledge, native `oak-explorer.toml`, and shared Codex adaptor. The native TOML decodes to the exact canonical explorer text and requires no external instruction file, launcher or registry. The portable coordinator remains a separate OAK scope. The repository fixtures exercise actual exact-tool dispatch, synchronized concurrency, immediate JOIN and rejection before synthesis; they are not Codex sessions.

The maintained adaptor is the sole source for its generated agent-bundle copy and authoring copy. The guide distinguishes manual placement, native defaults, parent runtime overrides, host-controlled tools, and unverified installation/discovery/enforcement. Both authoring forms receive the same orchestration and adaptor knowledge through explicit typed action bindings. No installed native client is required or claimed.

Primary documentation rechecked for the artifact contract on 2026-09-07:
- https://learn.chatgpt.com/docs/agent-configuration/subagents
- https://learn.chatgpt.com/docs/config-file/config-reference

## Size reduction without discarded teaching

The standalone delivery is 63,844 bytes, under the unchanged 64,000-byte limit. The skill entry is 8,373 bytes, under the unchanged 10,000-byte limit. These are UTF-8 file-byte counts, not character counts or estimates.

Four shape definitions and their separately formatted instances were duplicates of complete knowledge already in the literal teaching corpus. The schema design action now receives the entire exact teaching mapping. Eight original teaching documents are preserved byte-for-byte; the shape-gallery document still contains all four full schemas and populated instances. Independent baseline hashes, full schema/value comparisons and action-trace checks verify content and routing. Nothing is externalized from the standalone agent or encoded in a new opaque format.

All 41 package authoring rules remain verbatim with one guide owner each. The native ACT, exact-tool, parallel and delegated-document rules now belong to the orchestration guide. Duplicate lifetime/reading statements were consolidated into the unchanged part responsibility table, verbatim package lifetime rules, explicit local-frame guidance and ordered action routing. The template and grammar bytes are unchanged.

Fusion uses compact generated `gN-` support namespace prefixes rather than `guide-N-`. Descriptive entry suffixes, stable source ordering, identity separation, typed-only rewriting and collision rejection remain unchanged. This is the only functional diff in the build-only fusion module, not a new core language rule. The code and original collision test changed together after recording the owning build policy.

The skill version changes from 3.1.0 to 3.2.0. The optional validator helper has no other source change: its implementation, immutable runtime revision, package/dependency fingerprints and installation-consent safeguards are preserved. Arguments and exit codes are retained in the exact embedded helper; explanatory policy and real consent branches remain explicit. Existing optional-validator and skill/agent execution-parity tests remain active.

The detailed byte/meaning comparisons are in [d03-preservation.json](d03-preservation.json). They additionally verify unchanged root, core, dependency declarations, existing workflows and legacy APS files.

## Findings corrected during this milestone

1. The recovered build policy was not in its canonical render after a prose edit. It was canonicalized without altering policy meaning.
2. Checkpoint 01's context helper had two native actions in one preparation process. Specialist and adaptor reads now share one knowledge-loading action with both explicit inputs. The existing lifecycle ownership/replay/approval checks were not weakened; root lifecycle code and knowledge remain unchanged.
3. Native boolean validation now rejects `agents.enabled = 0`, which ordinary Python dictionary equality would otherwise equate with `false`.
4. Artifact verification covers an inherited source-directory symlink as well as a source-file symlink and operational-scope injection. Rejected sources and other generators' sentinels remain unchanged.
5. The supplementary statement check found a starred assertion-list expansion in the recovered worker's IF body, contrary to the existing named-statement assembly contract. Findings, evidence and coverage assertions now have explicit names assembled directly in the body. Canonical worker bytes are unchanged, and the original flat-assembly check passes without modification.
6. Cold generation now includes the maintained adaptor source and the five-file generator. Repair, exact inventories and unsafe-path checks cover the new bundle through the existing generator framework.

## Verification boundary

Checkpoint 02 includes exact command receipts, logs and complete tested file manifests for native artifacts and synchronized fixtures, authoring content/routing/parity, the optional validator, cold generation/repair/detached products, catalogue examples, scoped knowledge and lifecycle, and supplementary affected-source checks. Failed intermediate receipts are historical, not substituted for final passes. File hashes identify which commands cover the final tree.

The cloud environment uses Python 3.13.5, pydantic 2.13.4, pydantic-settings 2.14.1, pydantic-extra-types 2.11.1 and PyYAML 6.0.3. `pydantic-settings` is below the declared minimum of 2.15. No package was installed and no minimum was weakened. Successful local checks are useful observations, not certification of the full declared-dependency environment. The controller must repeat the final checks in its supplied environment during D05. No live Codex, permission-enforcement, desktop, network-bootstrap or PR check is claimed here.

## Remaining work

D04 must implement the actual SMEAC Directory Changes template and canonical sibling, schema-derived prospective checker, positive/negative specimens and complete directory reconciliation. The policy and this plan's visible tree are not a substitute for that implementation.

D05 must complete check registration for D04, run both complete repository entry points against the final source and declared dependencies, repeat generation and reconcile every comparison with the final diff, then perform a separate whole-task review. The D03 review does not preempt that final review.

D06 must produce the final report/evidence, preserve history when committing, verify remote bytes and create a non-draft PR targeting main. The controller must return the actual PR URL and check results before PR completion can be recorded. No merge is authorized.
