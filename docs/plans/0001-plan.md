# OAK Interface Flow Implementation Plan

```yaml
status: complete
change_type: language-and-runtime simplification
repository: chris-buckley/open-agent-knowledge
target_path: docs/plans/0001-plan.md
baseline_branch: main
baseline_commit: 7aefe22375b2343a64f64f28799c1b0cd3153c94
baseline_commit_time_utc: 2026-09-03T03:53:04Z
baseline_commit_time_brisbane: 2026-09-03T13:53:04+10:00
delivery: one complete implementation
design_status: accepted
prd_wording_approval: accepted
compatibility_layer: forbidden
new_test_suite: forbidden
```

This plan replaces direction-based grouped interfaces with terse one-way boundary contracts. It also removes ambient interface values and repeated identity bindings.

The phases are checkpoints inside one implementation. They are not versions or separate deliveries.

---

## 1. Objective

Make each interface one compact declaration of one complete schema instance crossing the active document boundary.

The final dataflow is:

```text
outside instance
  -> RECEIVES interface
  -> source-backed trigger
  -> process-local bindings
  -> process
  -> EMITS interface
  -> outside instance
```

---

## 2. Accepted contract

### Interface surface

```oak
<interfaces>
review-request RECEIVES schema.amendment-review-request
review-result EMITS schema.amendment-review: "Returned only to the successor coordinator."
</interfaces>
```

- `RECEIVES` and `EMITS` are the complete closed interface flow set.
- One interface entry occupies one line.
- Consecutive entries use one LF with no blank separator.
- The interface id appears once inside the interfaces part.
- References from other parts remain qualified as `interface.<id>`.
- The schema target can be local or relative.
- Text after `: ` is one optional JSON string.
- A description adds boundary meaning absent from the id and schema.
- `in`, `out`, and `inout` no longer exist.
- A duplex relationship uses two interfaces.

### Trigger handoff

```oak
<triggers>
trigger.amendment-review-requested.event := "An amendment review is requested."
trigger.amendment-review-requested.source := interface.review-request
trigger.amendment-review-requested.process := process.review-amendment
</triggers>
```

- A source must target one local `RECEIVES` interface.
- A source-backed trigger has no seeds.
- Its selected process must declare an input schema.
- The receive interface schema and process input schema must resolve to the same schema entry.
- The received instance becomes the complete process input.
- Event-only triggers retain explicit seeds from literals, constants, and state.
- Trigger events remain required semantic signposts.

### Process values and emission

```oak
<processes>
<process id="review-amendment" name="Review amendment" input="schema.amendment-review-request" output="schema.amendment-review">
ACT output="schema.amendment-review": Review <AMENDMENT> and produce <DECISION>. (AMENDMENT=$AMENDMENT) -> DECISION
EMIT interface.review-result
</process>
</processes>
```

- Process values are limited to literal, constant, state, and binding.
- `$interface.<id>.<placeholder>` no longer exists.
- `EMIT interface.result` fills the target schema from same-named visible bindings.
- `EMIT interface.result (RESULT=$FINAL_RESULT)` remains available for explicit projection.
- An explicit emit binding set must be non-empty and equal the interface schema placeholders.
- `EMIT interface.result ()` is invalid.

### Arrival API

```python
Arrival(
    interface="interface.review-request",
    values={"AMENDMENT": "..."},
)
```

- One arrival contains exactly one event or one local receive interface.
- An event arrival carries no values.
- A receive arrival carries one complete value mapping for its interface.
- The old `source` plus arbitrary `interfaces` mapping is removed.

### Generated interpretation instructions

Generate each applicable line once, in this order:

```text
RECEIVES accepts one complete instance of its schema.
A source-backed trigger supplies the received instance as the selected process input.
EMITS publishes one complete instance of its schema.
EMIT without bindings fills the target schema from same-named visible process bindings.
Text after `: ` states boundary meaning absent from the interface schema.
```

- Select each line from actual node features.
- Do not repeat interface ids, schemas, or descriptions in the instructions.
- Omit the description line when no interface has a description.
- Remove the old generic interface direction instruction.

---

## 3. Scope limits

* [x] Keep the seven parts unchanged.
* [x] Keep schemas independent of boundary use.
* [x] Keep triggers responsible for outside routing.
* [x] Keep processes responsible for local work.
* [x] Keep interface targets local to the active document.
* [x] Keep tool transport, credentials, channels, URLs, and server settings outside OAK.
* [x] Keep state and emission transaction semantics unchanged.
* [x] Keep unrelated syntax, models, error behaviour, and outputs unchanged.
* [x] Add no aliases, migration parser, fallback, or compatibility wrapper for the old interface forms.
* [x] Do not modify `legacy-snapshot-aps` or historical `docs/plans/0000-*` files.

---

## 4. Affected paths

| Concern | Paths |
|---|---|
| Authority | `AGENTS.md`, `docs/PRD.md`, `docs/plans/0001-plan.md` |
| Models | `oak/node/parts/interfaces.py`, `triggers.py`, `processes/values.py`, `processes/steps.py` |
| Validation | `oak/node/validation/flow.py`, `values.py`, `contracts.py`, `processes.py`, `triggers.py`, `node.py` |
| Vocabulary | `oak/vocabulary/text/dotted_path.py`, `value_reference.py` |
| Parsing | `oak/parse/interfaces.py`, `steps.py`, `values.py`, `grouping.py`, `document.py`, `fragments.py` |
| OAK render | `oak/render/oak/interfaces.py` new, `arrangement.py`, `groupings.py`, `instructions.py`, `processes.py` |
| JSON-LD | `oak/render/json_ld/context.py`, `entries.py`, `values.py` |
| Resolution | `oak/resolve/contracts.py`, `references.py` |
| Execution | `oak/execute/models.py`, `context.py`, `executor.py`, `values.py`, `steps.py` |
| Surfaces and rules | `oak/surface/entries.py`, `processes.py`, `registry.py`, `oak/rules/guidance.py`, `validation.py` |
| Public API | `oak/__init__.py`, `oak/node/parts/__init__.py`, `oak/execute/__init__.py` |
| Build | `build/authoring.py`, `docs.py`, `ebnf.py`, `surfaces.py`, `build/checks/*` |
| Examples | `examples/agents/*`, affected helpers, every sibling `.oak.md` |
| Outputs | `outputs/authoring.md`, `outputs/oak.ebnf`, affected `outputs/docs/*` |

The implementation inventory must add any dependent file omitted from this table.

---

## 5. Execution phases

### Phase 1. Authority and baseline

* [x] Create an isolated branch or worktree from the pinned baseline.
* [x] Read the root `AGENTS.md` in full.
* [x] Discover and read every nested `AGENTS.md` for affected paths.
* [x] Read `docs/PRD.md` in full.
* [x] Read the applicable Pydantic, JSON Schema, and JSON-LD skills and references.
* [x] Inventory every use of `Direction`, `direction`, `inout`, `InterfaceValue`, `$interface.`, `Arrival.source`, `Arrival.interfaces`, grouped interface entries, source-trigger seeds, and emit suffixes.
* [x] Run `python build/examples.py` and record the untouched baseline result.
* [x] Record the generated output and example snapshot path sets.
* [x] Prepare one runnable Python example, its generated OAK, and exact PRD line replacements.
* [x] Present the exact PRD and `AGENTS.md` changes and obtain wording approval before editing them.

### Phase 2. Product requirements and concepts

* [x] Redefine `interface` in the `AGENTS.md` concepts CSV as one identified one-way boundary crossing that selects a flow and schema.
* [x] Update the `trigger` concept when needed to state complete receive handoff.
* [x] Update PRD Constraints for flow, source handoff, arrivals, process values, and inferred emission.
* [x] Update PRD Structure, Triggers, Processes, Interfaces, Vocabulary, Authoring, Render, JSON-LD, Build, Examples, and Tree sections.
* [x] Define the exact canonical interface grammar and separators.
* [x] Define the generated instruction predicates and order.
* [x] Define stable validation, parse, resolution, and execution error codes for the new rules.
* [x] Delete obsolete direction, interface-read, and multi-interface-arrival requirements.
* [x] Add `docs/plans/0001-plan.md` and the planned `0001-report.md` to the PRD tree.
* [x] Keep every unrelated PRD line unchanged.

### Phase 3. Models, vocabulary, and public API

* [x] Define `InterfaceFlow` as the closed values `receives|emits`.
* [x] Define one ordered interface-flow registry for surface keywords and interpretation text.
* [x] Replace `Interface.direction` with `Interface.flow` and update metadata examples.
* [x] Keep `Interface.schema` and optional `description` typed as today.
* [x] Remove `Direction`, `in`, `out`, and `inout` from production exports and schemas.
* [x] Remove `InterfaceValue` from the model, `Value` union, traversal, metadata, and exports.
* [x] Remove interface placeholder paths from `DottedPath` and `ValueReference`.
* [x] Permit `Emit.bindings=[]` as the inferred form while keeping explicit lists typed.
* [x] Require source-backed trigger seeds to be empty.
* [x] Replace `Arrival.source` and `Arrival.interfaces` with `Arrival.interface` and `Arrival.values`.
* [x] Enforce exactly one event or interface arrival shape.
* [x] Update every root and package export without a compatibility alias.

### Phase 4. OAK surface, parsing, rendering, and instructions

* [x] Add `oak/render/oak/interfaces.py` as the sole interface text renderer.
* [x] Render `<ID> RECEIVES <SCHEMA>` or `<ID> EMITS <SCHEMA>`.
* [x] Append `: <JSON_STRING>` only when a description exists.
* [x] Render consecutive interface entries with one LF and no blank separator.
* [x] Keep the interface part body byte-identical between XML and Markdown groupings.
* [x] Remove interface entry wrappers from both groupings.
* [x] Remove interface from grouped entry tags and grouped-entry parsing.
* [x] Parse interface entries line by line and reject blank separators.
* [x] Reject an unknown flow, malformed schema target, malformed JSON description, or extra text.
* [x] Define two interface surface descriptors selected by `flow`.
* [x] Render bindingless emit without a suffix.
* [x] Parse bindingless and explicit emit forms and reject an empty `()` suffix.
* [x] Remove interface value parsing and rendering.
* [x] Generate the five accepted interpretation lines only when their predicates are true.
* [x] Add every possible generated line to built-in instruction stripping.
* [x] Update EBNF and fragment parsing from the same surfaces.

### Phase 5. Local validation and resolution

* [x] Require every trigger source to target one local `RECEIVES` interface.
* [x] Require every emit target to be one local `EMITS` interface.
* [x] Reject seeds on a source-backed trigger.
* [x] Require a source-backed selected process to declare an input schema.
* [x] Compare receive-interface and process-input schemas by resolved document and schema id.
* [x] Perform the same-schema check locally when all targets are local.
* [x] Defer only relative identity checks to explicit resolution.
* [x] Keep event-trigger seed contract validation unchanged except for removed interface values.
* [x] Validate explicit emit bindings against the complete interface schema.
* [x] Validate inferred emit placeholders against bindings visible at that exact step.
* [x] Reuse one binding-scope traversal for top-level, branch, loop, and parallel visibility.
* [x] Validate relative interface schemas for inferred and explicit emits.
* [x] Remove obsolete interface-read and direction checks.
* [x] Keep trigger overlap, call-cycle, branch, tool, and transaction checks unchanged.

### Phase 6. Execution

* [x] Remove the transaction-wide active-interface mapping from `ExecutionContext`.
* [x] Validate one receive arrival against its local receive interface and resolved schema before trigger selection.
* [x] Reject values on an event arrival.
* [x] Match event triggers only from event arrivals.
* [x] Match source-backed triggers only from the exact receive interface arrival.
* [x] Pass receive values directly as the selected process inputs.
* [x] Resolve explicit seeds only for event-backed triggers.
* [x] Remove runtime interface value resolution and missing-interface-value errors.
* [x] Build inferred emission values from same-named visible bindings in schema order.
* [x] Keep explicit emission binding resolution unchanged.
* [x] Validate every emitted instance before staging it.
* [x] Keep state and emissions staged until successful top-level completion.
* [x] Keep no-match, ambiguous-match, tool, loop, parallel, and caller-state behaviour unchanged.

### Phase 7. JSON-LD, surfaces, docs, and outputs

* [x] Render each interface with `flow`, id-valued `schema`, and optional `description`.
* [x] Remove InterfaceValue JSON-LD encoding and vocabulary terms.
* [x] Omit empty emit bindings from JSON-LD and treat omission as inferred binding.
* [x] Update the JSON-LD context and target typing.
* [x] Remove `InterfaceValue` from `AUTHORABLE_MODELS` and generated documentation.
* [x] Delete stale `outputs/docs/interface-value.md`.
* [x] Generate both interface flow surfaces on `outputs/docs/interface.md`.
* [x] Update emit, trigger, node, and related generated pages.
* [x] Update `outputs/authoring.md` with the new grammar, instructions, examples, and interface declaration.
* [x] Regenerate `outputs/oak.ebnf`.
* [x] Add a freshness gate that equates the `InterfaceFlow` values, surface flows, parse keywords, render keywords, and flow-instruction keys.
* [x] Require the final generated path set to contain no stale page.

### Phase 8. Examples and migration

* [x] Inspect every source-backed trigger before changing it.
* [x] Prove that each source interface schema equals the selected process input schema.
* [x] Add the smallest typed adapter process only when those schemas differ by meaning.
* [x] Replace every interface constructor `direction=` with `flow=`.
* [x] Remove source-trigger identity seeds.
* [x] Replace old arrivals with one event or one interface plus values.
* [x] Remove every `InterfaceValue` and `$interface.` read.
* [x] Use bindingless `EMIT` only when same-named visible bindings satisfy the complete target schema.
* [x] Keep explicit `EMIT` bindings for projection or renaming.
* [x] Remove `interface_bindings` from `examples/agents/bindings.py` when no use remains.
* [x] Shorten direction-only interface id suffixes when the shorter id stays unambiguous.
* [x] Update `build/authoring.py` and its generated example.
* [x] Migrate `amendment_reviewer.py` and its snapshot.
* [x] Migrate `compound_growth.py` and its snapshot.
* [x] Migrate `delegation.py` and its snapshot.
* [x] Migrate `implementer.py` and its snapshot.
* [x] Migrate `successor.py` and its snapshot.
* [x] Migrate `successor_verifier.py` and its snapshot.
* [x] Migrate `task_reviewer.py` and its snapshot.
* [x] Run every Python example and regenerate each sibling `.oak.md` through its own authoring path.

### Phase 9. Verification, cleanup, and report

* [x] Add positive checks for both flows with local and relative schemas.
* [x] Add positive checks for descriptions present and absent.
* [x] Add positive round trips for both groupings.
* [x] Add positive checks for event seeds and source payload handoff.
* [x] Add positive checks for inferred and explicit emission.
* [x] Add a positive duplex example using two one-way interfaces.
* [x] Prove each generated interpretation line appears once when used and zero times when unused.
* [x] Reject old grouped interface entries and old direction values.
* [x] Reject `inout`.
* [x] Reject source triggers that target `EMITS`, contain seeds, lack process input, or use a different resolved schema.
* [x] Reject `$interface.<id>.<placeholder>` in every process value position.
* [x] Reject the old multi-interface arrival shape.
* [x] Reject emission through `RECEIVES`.
* [x] Reject inferred emit when one required binding is not visible.
* [x] Reject explicit emit with missing, duplicate, or extra placeholders.
* [x] Reject `EMIT target ()`.
* [x] Run `python build/examples.py` to completion.
* [x] Confirm every committed example parses and reproduces its canonical render.
* [x] Confirm generated outputs equal a fresh build.
* [x] Confirm JSON Schema, JSON-LD, controlled style, resolution, and execution checks pass.
* [x] Search production, build, examples, and current outputs for obsolete syntax and identifiers.
* [x] Exclude only the legacy snapshot and historical `0000-*` documents from that search.
* [x] Inspect the final diff for accidental changes and duplicate implementations.
* [x] Confirm no compatibility layer, stale output, dead helper, or unused export remains.
* [x] Write `docs/plans/0001-report.md` with the baseline, changed paths, checks, expected output changes, and final verdict.
* [x] Mark this plan complete only after every applicable checkbox is satisfied.
Post-merge: delete the implementation branch on the remote and locally.

---

## 6. Completion standard

The implementation is complete only when:

* [x] The accepted OAK examples render exactly as specified in this plan.
* [x] All valid documents use one-line `RECEIVES|EMITS` interface entries.
* [x] No process can read ambient interface values.
* [x] Every source-backed trigger performs an exact typed payload handoff.
* [x] Every bindingless emit is statically and dynamically safe.
* [x] Generated instructions describe only features present in the node.
* [x] The full build passes with no stale files.
* [x] The final report verdict is `Approved`.
